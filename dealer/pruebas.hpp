#include <iostream>
#include <string>
#include <vector>
#include <list>
#include <random>
#include <chrono>


class motor{
public: 
    void std::gametype();
private:
    int idplayer;
    std::vector<std::string> players;
    std::list<std::string> games;
    std::string namecode;
    std::string nameplayer;
    int montoplayer;


}